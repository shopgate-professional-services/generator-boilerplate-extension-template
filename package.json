{
  "name": "@<%= extension.organization %>/<%= extension.name %>",
  "version": "0.0.1",
  "description": "",
  "license": "<%= extension.licence %>",
  "scripts": {
    <% if (backend.active) { %>"precommit:extension": "cd extension && npm run precommit",<% } %>
    <% if (frontend.active) { %>"precommit:frontend": "cd frontend && npm run precommit",<% } %>
    "precommit": "npm-run-all precommit:*"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "npm-run-all": "^4.1.2"
  },
  "dependencies": {}
}
